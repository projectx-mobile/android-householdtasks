<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:drawable="@drawable/ic_error"></item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <stroke
                android:width="2dp"
                android:color="@color/error_color" />
            <size android:height="56dp" />
            <padding
                android:bottom="@dimen/spacing.small"
                android:left="14dp"
                android:right="@dimen/spacing.small"
                android:top="@dimen/spacing.small" />
            <corners android:radius="@dimen/spacing.small" />
        </shape>
    </item>
</layer-list>
